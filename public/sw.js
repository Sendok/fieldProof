const SHELL_CACHE="fieldproof-shell-v1";
const TASK_CACHE="fieldproof-tasks-v1";
self.addEventListener("install",(event)=>event.waitUntil(caches.open(SHELL_CACHE).then((cache)=>cache.addAll(["/"])).then(()=>self.skipWaiting())));
self.addEventListener("activate",(event)=>event.waitUntil(self.clients.claim()));
self.addEventListener("message",(event)=>{if(event.data?.type==="CLEAR_PRIVATE_CACHES")event.waitUntil(Promise.all([caches.delete(SHELL_CACHE),caches.delete(TASK_CACHE)]));});
self.addEventListener("fetch",(event)=>{const request=event.request;if(request.method!=="GET")return;const url=new URL(request.url);if(url.origin!==self.location.origin)return;if(url.pathname.startsWith("/_next/static/")){event.respondWith(caches.open(SHELL_CACHE).then(async(cache)=>await cache.match(request)||fetch(request).then((response)=>{if(response.ok)void cache.put(request,response.clone());return response;})));return;}const isTask=url.pathname.startsWith("/app/my-tasks")||/^\/app\/work-orders\/[^/]+\/execute$/.test(url.pathname);if(!isTask)return;event.respondWith(fetch(request).then((response)=>{if(response.ok){const copy=response.clone();void caches.open(TASK_CACHE).then((cache)=>cache.put(request,copy));}return response;}).catch(async()=>{const cached=await caches.match(request);return cached||new Response("Tugas ini belum tersedia offline.",{status:503,headers:{"content-type":"text/plain; charset=utf-8"}});}));});
